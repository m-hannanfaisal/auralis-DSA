# 🔊 Auralis — Trading Strategy Optimization via DSA & Machine Learning


Combining custom Data Structures with XGBoost signal filtering to turn a +15.58R baseline into a +94.92R optimized strategy — while maintaining controlled risk.




## 📌 Project Overview

Auralis is a research-grade algorithmic trading system built as a dual-domain project — applying core Data Structures & Algorithms for efficient real-time data management, and Machine Learning for intelligent trade signal filtering.

The system ingests historical candlestick data, generates trading signals using trend and volatility indicators, filters them through an XGBoost classifier, and backtests the result against four rule-based baselines.





## 🏆 Key Results

StrategyNet R-MultipleNotesBaseline A+15.58RSimple breakoutBaseline B+22.31RBreakout + Asia filterBaseline C+18.44RRetracement entryBaseline D+21.07RCombined rulesProposed (XGBoost)+94.92RML-filtered signals


R-Multiple = return normalized by initial risk per trade. Higher = better risk-adjusted performance.




# 🧠 Dual Domain: DSA + AI/ML

## 📐 Data Structures & Algorithms

Custom-built structures designed for real-time trading data:

ModuleStructure UsedPurposedsa/candle_store.pyDynamic Array + Binary SearchFast O(log n) candle retrievaldsa/session_manager.pyHash Map + AVL TreeSession-based time window managementdsa/trade_manager.pyMin-HeapPriority-based trade risk trackingbenchmark_dsa.py—Empirical complexity benchmarking

Benchmarks verify theoretical complexities (O(log n) inserts, O(1) lookups) against real data sizes up to 100,000 candles.

## 🤖 Machine Learning (XGBoost)

ComponentDetailModelXGBoost Binary ClassifierFeaturesTrend indicators, volatility ratios, session contextLabelsProfitable trade = 1, Losing trade = 0 (generated by pipelines/labeler.py)FilterSignals with confidence ≥ threshold passed to backtesterThreshold SweepTested at 5%, 30%, 40%, 50%, 60%, 70%, 80%, 95%


## 🗂️ Project Structure

AuralisCode/
│
├── dsa/                          # Core DSA implementations
│   ├── candle_store.py           # Dynamic array with binary search
│   ├── session_manager.py        # AVL tree-based session tracking
│   └── trade_manager.py          # Min-heap for risk management
│
├── models/                       # ML model definitions
│   ├── model_proposed_v2.py      # Final proposed model
│   ├── train_xgb_filter.py       # XGBoost training pipeline
│   └── xgb_filter.py             # Inference wrapper
│
├── pipelines/                    # Data & backtest pipelines
│   ├── labeler.py                # Signal labeling for ML training
│   ├── build_signals_dataset.py  # Feature engineering
│   ├── signals_v2.py             # Signal generation v2
│   └── backtest_baseline_optimized.py
│
├── model_baseline_A/B/C/D.py     # Four rule-based baselines
├── model_proposed.py             # Proposed ML-enhanced strategy
├── data_loader.py                # Data ingestion
├── preprocess.py                 # Feature preprocessing
├── evaluate.py                   # Strategy evaluation
├── benchmark_dsa.py              # DSA complexity benchmarks
├── auralis_dsa.py                # DSA demonstration
├── run_experiment.py             # Experiment runner
│
├── results/                      # CSV outputs per experiment
├── reports/                      # Text reports & benchmark logs
├── plots/                        # Performance & DSA figures
│   ├── baseline_A/B/C/D/         # Per-baseline equity curves
│   ├── dsa_figures/              # AVL tree, heap, complexity plots
│   └── proposed_v2/              # Feature importance, threshold sweep
│
└── requirement.txt


## ⚙️ Installation & Setup

bash# Clone the repo
git clone https://github.com/YOUR_USERNAME/auralis-dsa.git
cd auralis-dsa

## Install dependencies
pip install -r requirement.txt


🚀 Running the Project

# 1. Run DSA benchmarks & demonstrations
python auralis_dsa.py
python benchmark_dsa.py

# 2. Build signal dataset & labels
python pipelines/build_signals_dataset.py
python pipelines/labeler.py

# 3. Train the XGBoost model
python models/train_xgb_filter.py

# 4. Run full experiments (all baselines + proposed)
python run_experiment.py
python run_experiments_v2.py

# 5. Evaluate & generate plots
python evaluate_results_v2.py
python generate_dsa_figures.py


📊 Sample Plots

DSA BenchmarksStrategy PerformanceAVL Tree structureEquity curve (proposed)Heap operationsFeature importanceComplexity comparisonThreshold sweepDynamic array growthMonthly performance

(All plots saved to plots/ after running the experiment pipeline)


## 🛠️ Tech Stack


Language: Python 3.13
ML: XGBoost, scikit-learn
Data: Pandas, NumPy, PyArrow (Parquet)
Visualization: Matplotlib, Seaborn
DSA: Custom implementations (no external DSA libraries)



## 📄 Report

A full LaTeX report (DSA_Report_Template.tex) documents:


DSA design decisions and complexity proofs
ML feature engineering methodology
Backtest results and statistical analysis
Comparison against all baselines




## 📜 License

This project was developed as an academic assignment. All code is original unless otherwise noted.
